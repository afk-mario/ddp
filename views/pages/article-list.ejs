<!DOCTYPE html>
<html lang="en">
    <head>
        <% include ../partials/head %>
        <meta property="og:image:height" content="1257">
        <meta property="og:image:width" content="2400">
    </head>
    <body class="container">
        <%- include('../partials/header', { podcast: podcast }); %>
        <main class='article-list-main'>
            <div class="wrapper">
                <div class="articles-info">
                    <h1 class="main-title">Blog</h1>
                    <div class="blog-description">
                        <p>
                            Lorem ipsum dolor sit amet, consectetur adipiscing elit. Praesent interdum gravida consectetur. Mauris vitae nibh aliquam diam bibendum scelerisque. Aenean aliquet tellus a urna viverra, vel porttitor nisl pulvinar. Donec vitae metus enim. Curabitur ut sapien aliquet, euismod quam ac, pulvinar odio. In eu odio dapibus, tincidunt diam non, interdum mi. Pellentesque vitae mollis lorem. Nullam eu convallis lorem. Etiam gravida tempor mauris et feugiat. Fusce rhoncus non arcu non condimentum.
                        </p>
                    </div>
                </div>
                <section class="article-list">
                    <% list.forEach(function(article, i){ %>
                        <%- include('../partials/article-row', {
                            article: article,
                            className: i === 0 ? 'active' : '',
                        }); %>
                <% }); %>
                <% list.forEach(function(article, i){ %>
                    <%- include('../partials/article-row', {
                        article: article,
                        className: i === 0 ? 'active' : '',
                    }); %>
            <% }); %>
            <% list.forEach(function(article, i){ %>
                <%- include('../partials/article-row', {
                    article: article,
                    className: i === 0 ? 'active' : '',
                }); %>
        <% }); %>
                </section>
            </div>
        </main>
    </body>
</html>
