<!DOCTYPE html>
<html lang="en">
    <head>
        <% include ../partials/head %>
        <meta property="og:image:height" content="1257">
        <meta property="og:image:width" content="2400">
    </head>
    <body class="container">
        <%- include('../partials/header', { podcast: podcast }); %>
        <main class='acerca-de'>
            <div class="wrapper">
                <div class="podcast-info">
                    <img class="logo" src="<%= podcast.image %>" alt="<%= podcast.title %>">
                    <% include ../partials/podcast-links %>
                </div>
                <div class="rich-text">
                  <%- podcast.md %>
                </div>
            </div>
        </main>
    </body>
</html>
